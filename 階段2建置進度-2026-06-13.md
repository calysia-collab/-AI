# 莎莎保險助理工作台

## 階段 2 建置進度

更新日期：2026-06-13

## 已完成

- 新增不可變 PostgreSQL `004_phase2_domain_model.sql` 與雜湊鎖。
- 新增客戶資料、聯絡方式、關係人、保障項目、保單關係人、互動紀錄、任務、文件及同意紀錄資料表。
- 核心客戶、保單、行程及團隊資料加入軟封存與必要索引。
- SQLite 與 PostgreSQL 維持相同的階段 2 結構。
- 建立 `/api/v1` 客戶、保單及行程 API。
- 建立游標分頁、允許清單篩選、排序、ETag／`If-Match` 衝突控制。
- 建立封存與還原流程。
- 建立客戶匯入預覽、驗證及執行流程。
- 建立有權限控管及稽核紀錄的分頁匯出。
- 建立獨立團隊狀態 API；舊 `/api/state` 已改為唯讀。
- 前端客戶、保單及行事曆寫入已切換至 `/api/v1`。
- 建立階段 2 資料表、筆數及組織範圍核對工具。
- 建立 10 萬筆客戶游標分頁與索引壓力測試。
- 建立 PostgreSQL 回滾腳本及 API 操作文件。
- 建立客戶 360 畫面，串接聯絡方式、關係人、互動、任務、文件與同意紀錄。
- 建立 `005_phase2_automation.sql`、加密盲索引及全域搜尋。
- 建立 CSV／XLSX 50,000 筆背景匯入、進度、取消與錯誤檔下載。
- 匯入工作在服務重啟後會從已完成位置自動續跑。
- 建立 PostgreSQL 10 萬筆分頁與盲索引查詢驗證工具。

## 本輪驗收

- 本機資料庫完整性檢查通過。
- PostgreSQL 遷移雜湊檢查通過。
- 新版 API 建立、修改、衝突、分頁、封存、還原、匯入預覽與匯出測試通過。
- 舊 API 回傳契約維持相容。
- 顧問客戶範圍與組織隔離測試通過。
- Docker staging 驗證於 2026-06-13 17:07 全數通過。
- PostgreSQL `004`、API v1、ClamAV、EICAR、HTTPS、Secure Cookie、附件下載、資料保護及 `pg_dump/pg_restore` 均通過。
- 2026-06-13 最新本機正式版驗證：82 項自動測試全數通過。
- SQLite 10 萬筆測試：第一頁 4.04 毫秒、第二頁 4.45 毫秒，使用 `customers_active_page_idx`。
- 備份還原與資料／附件金鑰輪替演練通過，明文及舊金鑰殘留均為 0。
- 2026-06-18 Docker staging `outputs/staging-validation-20260618-012830` 全數通過。
- Docker staging PostgreSQL 10 萬筆測試：第一頁 0.264 毫秒、第二頁 0.205 毫秒、盲索引搜尋 0.111 毫秒。
- Docker staging 已驗證 Phase 2 schema、v1 API、客戶 360、背景匯入、搜尋、資料保護稽核與 `pg_dump/pg_restore`。

## 階段 2 結論

- 階段 2 程式建置與本機驗收已完成。
- Docker 自動 staging 已通過 PostgreSQL 10 萬筆、背景匯入、盲索引、OCR 及還原筆數核對。
- 正式受管理 PostgreSQL、正式 KMS、有效公開憑證及四平台實機簽署仍屬外部部署驗收，不是程式缺漏。
