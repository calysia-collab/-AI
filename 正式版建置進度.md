# 莎莎保險助理工作台正式版建置進度

## 2026-06-12：帳號安全與 PostgreSQL 資料層完成

- 新增首次啟動設定流程，由張經理建立唯一的工作台主人帳號。
- 密碼使用每位使用者獨立鹽值與 `scrypt` 雜湊，不保存明文密碼。
- 新增 HttpOnly、SameSite 登入 Cookie、12 小時有效期限與 30 分鐘閒置逾時。
- 新增 CSRF 防偽請求驗證，保護所有新增、修改與刪除 API。
- 密碼連續錯誤 5 次後暫時鎖定 15 分鐘。
- 新增 `owner`、`manager`、`advisor`、`viewer` 角色基礎；稽核與監控限主人或管理者。
- 未登入時客戶、保單、行程、同步、稽核與監控 API 一律拒絕存取。
- 新增安全登入頁、首次設定頁、登出按鈕與登出後敏感瀏覽器快取清除。
- PostgreSQL 正式版結構已補上組織、使用者與登入工作階段資料表。
- 主人可在團隊管理頁新增登入帳號、調整角色、停用／啟用帳號與重設密碼。
- 客戶、保單、行程、團隊、任務、設定、版本號及稽核紀錄均依 `organization_id` 隔離。
- 舊版未歸戶資料會在首次設定或單一組織升級時自動歸入原工作台。
- 帳號停用、角色調整或密碼重設時，既有登入工作階段立即失效。
- 顧問不可使用整批同步 API 修改團隊資料；唯讀角色不可進行資料異動。
- SQLite 以觸發器阻擋跨組織保單與行程關聯。
- 已用實際 Edge 完成首次設定、登入、團隊頁與建立帳號互動驗證。
- 使用者可自行變更密碼；變更後所有舊登入工作階段立即失效。
- 新增驗證器 App TOTP MFA、加密秘密、驗證碼重播防護與 10 組一次性復原碼。
- MFA 復原碼可下載保存，離開設定視窗或完成啟用後會從畫面記憶體清除。
- 新增本機主金鑰建立與讀取；正式環境可由 `SASHA_MASTER_KEY` 注入 32 位元組金鑰。
- 錯誤 JSON 與過大請求會回傳受控錯誤，不再誤報為伺服器內部故障。
- 自動測試增至 62 項，全部通過。
- 後端登入與 API 已改為同步／非同步資料庫皆可使用，SQLite 操作維持相容。
- 新增 PostgreSQL 連線池、完整帳號／MFA／客戶／保單／行程／團隊／稽核資料層。
- 新增啟動前資料表遷移、跨程序鎖定、遷移校驗碼及重複執行保護。
- 新增 `verify-full` TLS、連線池大小與資料庫引擎健康狀態。
- 新增 SQLite 一致性快照轉移；只允許空白 PostgreSQL，並逐表驗證筆數。
- 轉移時不搬移既有登入工作階段，所有人須在新環境重新登入。
- 客戶新增穩定的 `owner_user_id` 負責人帳號識別；顧問改名時客戶顯示名稱會同步更新。
- 主人與管理者可指派客戶，顧問新增客戶時由後端強制指派本人，不能竄改成其他顧問。
- 顧問只能讀寫自己負責的客戶、相關保單及客戶行程；未綁定客戶的團隊行程維持團隊共用。
- SQLite 與 PostgreSQL 均在資料庫查詢層套用相同的顧問資料範圍。
- 舊客戶只有在顧問姓名可唯一對應同組織有效帳號時才自動補上負責人識別，避免錯誤歸戶。
- 新增僅能由主機維護人員執行的緊急帳號復原工具，不開放匿名密碼重設 API。
- 緊急復原會驗證新密碼強度、拒絕沿用舊密碼、清除帳號鎖定、使既有登入失效並留下稽核紀錄。
- 若驗證器與 MFA 復原碼同時遺失，可用明確參數停用 MFA 並清除舊復原碼。

### 下一步

- 串接 Email 或企業身分驗證服務，提供可遠端核身的忘記密碼流程；本機緊急復原已完成。
- 在受管理 PostgreSQL 實例安裝 `pg` 驅動並完成一次實際連線、遷移與回復演練。
- 由雲端金鑰服務管理加密金鑰。

更新日期：2026 年 6 月 12 日

## 已完成

### 第一階段：安全與工程基礎

- 使用者輸入、新聞、法規、商品及文件資料的 HTML 輸出安全處理。
- 外部網址只允許 HTTPS。
- 瀏覽器資料損壞時自動回復預設資料。
- 舊版 `obsidian-*` 資料鍵自動遷移至 `sasha-*`。
- 保單完整保存保單號碼、契約始期、繳費年期及保障摘要。
- 保單與行程加入 `customerId` 關聯。
- 客戶保單數改由實際保單計算。
- 刪除客戶與移除團隊成員前檢查關聯資料。
- PWA Manifest、App 圖示、Service Worker 及離線殼層。
- 本機安全網站伺服器、CSP、禁止嵌入及基本安全標頭。
- 手機通知入口、鍵盤焦點、日曆鍵盤操作及減少動態效果。

### 第二階段：後端與資料庫基礎

- `/api/health` 系統健康檢查。
- `/api/state` 讀取及同步應用程式資料。
- 客戶、保單、行程、團隊及任務的關聯式資料表。
- 保單外鍵與行程客戶關聯。
- 同步修訂版號及 409 衝突保護。
- 單次請求大小、資料筆數、必要欄位及重複 id 驗證。
- 本機資料庫第一次啟動自動匯入。
- 離線時保留瀏覽器資料，恢復連線後可重新連接。
- 在登入權限完成前，伺服器預設拒絕非本機連線。
- PostgreSQL 正式資料表、非同步資料層、遷移器及 SQLite 轉移工具。
- 客戶負責人帳號關聯、顧問指定客戶範圍及跨客戶存取阻擋。

### 第三階段：逐筆資料操作與行程流程

- 客戶、保單及行程個別 REST API。
- 單筆新增、讀取、修改及刪除。
- 每筆資料版本號與舊版本衝突保護。
- 行程新增、編輯、完成、取消及刪除。
- 月曆與今日時間軸可直接開啟行程。
- 行程離線待同步佇列。
- 客戶與保單離線狀態保護。
- 避免背景整批同步與逐筆修改互相覆蓋。

### 營運韌性基礎

- `/api/metrics` 健康與使用量指標。
- `/api/audit` 隱私最小化操作稽核。
- SQLite 一致性備份。
- 備份完整性驗證。
- 還原前確認、安全副本及還原後驗證。
- 完整營運維護手冊。

## 驗證結果

- JavaScript 語法檢查通過。
- HTML 識別碼沒有重複或遺失引用。
- 核心工具、資料庫、外鍵、版本衝突、伺服器、安全標頭與來源碼安全測試通過。
- 目前自動測試：62 項通過。
- 實際 Edge 互動驗證：首次設定、登入解鎖、團隊頁、帳號建立、MFA 啟用、敏感資料清除、第二道驗證及復原碼登入通過。

## 尚未完成

### 第二階段後續

- 受管理 PostgreSQL 實例的真實連線、資料轉移及災難回復演練。
- 遠端核身忘記密碼流程、雲端 KMS、金鑰輪替與客戶敏感欄位加密。
- 手機與電腦的即時同步及可視化衝突解決。

### 第三階段

- 行程重複規則及完成後續任務。
- 客戶服務歷程、家庭關係、標籤、附件與重複客戶偵測。
- 保單主約、附約、受益人與保障項目明細。
- Excel／CSV 匯入匯出、PDF 報表及全文搜尋。

### 第四、五階段

- OCR、外部情報排程、通知、AI 語錄、電子簽署及保險公司入口。
- 管理後台、監控、操作稽核、備份還原、自動部署及法遵驗收。

## 目前使用限制

目前可作為具登入、MFA、角色權限、組織隔離與顧問指定客戶範圍的本機正式化開發版使用，但不可直接公開到網際網路，也不建議在完成客戶欄位加密與法遵驗收前存放正式客戶個資。正式對外部署仍須完成 PostgreSQL、HTTPS、雲端金鑰管理、集中監控與法遵階段。
